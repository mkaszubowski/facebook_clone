<h1>All users</h1>

<ul class="user-list">
  <%= for user <- @users do %>
    <div class="user">
      <li><%= full_name(user) %></li>
      <%= invite_button(@conn, user, @current_user) %>
    </div>
  <% end %>
</ul>
